import { useState } from "react";

const ImageSlider = ({slides}) => {
    const [currentIndex, setCurrentIndex] = useState(0);
    return (
        <div>
            <div className="bg-slate-900 dark:bg-slate-100"></div>
        </div>
    )
}
export default ImageSlider;